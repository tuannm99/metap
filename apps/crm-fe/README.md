# metap web (scaffold)

A temporary frontend scaffold for metap — see the root `CLAUDE.md`'s "Frontend (scaffold)" section for how to run it and how dev auth works.

```bash
pnpm install
pnpm dev
```
